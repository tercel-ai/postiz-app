/**
 * Quora post settings. A Quora feed/space post is a single text body with no
 * title, so there are no required settings — this exists to keep the provider's
 * `dto` shape consistent with the other providers. Publishing happens via the
 * browser extension (Quora has no API).
 */
export class QuoraSettingsDto {}
